fn main() {
    // let rt = tokio::runtime::Builder::new_current_thread()
    //     .enable_all()
    //     .build()
    //     .unwrap();

    // rt.block_on(async {});

    // let th = async {};
    // let mut ctx = std::task::Context::from(10);
    // while let std::future::Ready(it) = std::future::Future::poll(std::pin::Pin::from(th), &mut ctx)
    // {
    // }
}
